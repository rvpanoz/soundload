import { Arch } from "electron-builder-util";
import { PlatformPackager } from "../../platformPackager";
import { NsisTarget } from "./nsis";
import { NsisScriptGenerator } from "./nsisScriptGenerator";
export declare const nsisTemplatesDir: string;
export declare class AppPackageHelper {
    private readonly archToFileInfo;
    private readonly infoToIsDelete;
    /** @private */
    refCount: number;
    packArch(arch: Arch, target: NsisTarget): Promise<string>;
    finishBuild(): Promise<any>;
}
export declare function addCustomMessageFileInclude(input: string, packager: PlatformPackager<any>, isMultiLang: boolean, scriptGenerator: NsisScriptGenerator): Promise<void>;
